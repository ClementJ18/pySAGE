"""The `sage_lint` subcommand implementations, one module per command (`format`, `lint`,
`serve`, `diff`, `init`), with the option resolution and report formatting they share in
`common`. `sage_lint.cli` owns the argument parser and dispatches here."""
