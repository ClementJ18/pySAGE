"""The Edain Linter: a PyQt6 desktop app combining SAGE Lint's ini checks and the Edain map
checks in one tabbed window (see app.py). Built on the shared sage_utils widgets, the shared
FindingsView results table, and the shared theme."""

__version__ = "0.1.0"
